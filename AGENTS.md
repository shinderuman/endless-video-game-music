# AGENTS.md

## 1. 権限と正本

このプロジェクトでは、仕様の正本を次の順序で扱います。

1. `DESIGN.md`
2. 利用者が明示したGO・NO-GO・仕様変更
3. `POC.md`
4. `POC_RESULTS.md`
5. `references/approved-player-mockup.html`

文書間に矛盾を見つけた場合は、勝手に解釈して実装せず、矛盾箇所と影響だけを報告して停止してください。

## 2. 絶対的な段階ゲート

- PoCが明示的にGOになるまで製品実装を始めないでください。
- 各PoC段階を完了したら、結果を文書化して報告し、停止してください。
- 次の段階へ進むには利用者の明示的な許可が必要です。
- PoCに失敗した場合、代替実装を勝手に製品へ組み込まず、設計修正案またはNO-GOを先に報告してください。
- PoC用コードは `poc/` 配下に限定し、製品コードと混在させないでください。

## 3. エージェント自身が行うこと

必要な調査、コマンド実行、依存確認、ビルド、テスト、ブラウザ操作、エミュレーター操作、ログ確認、失敗原因の切り分けはエージェント自身が行ってください。

利用者へ次の作業を押し付けてはいけません。

- コマンドを実行して結果を返すこと
- テストを手動で代行すること
- エラー原因を調べること
- 依存ツールのヘルプを確認すること
- 生成物を目視だけで検証すること

利用者による聴感ラベル付け、GO判定、過去Codexセッションへの質問中継など、利用者にしかできない判断だけを依頼できます。

## 4. 要求されていない機能の禁止

`DESIGN.md`にない機能を追加しないでください。便利そう、一般的、将来必要そうという理由での追加は禁止です。

特に永久対象外に指定された機能は、設計案、PoC、製品コードのいずれにも追加してはいけません。手動ループ位置編集だけは将来TODO候補ですが、明示的に指示されるまで実装禁止です。

## 5. 過剰設計の禁止

- 不要な抽象化、汎用化、プラグイン機構、独自フレームワークを作らないでください。
- 発生頻度の低いエッジケースへ先回りしないでください。
- データベースを導入しないでください。
- 互換性レイヤーを複数並行実装しないでください。
- PoCで必要性が証明される前にパス照合とSHA-256照合を両方製品実装しないでください。
- 個人用ツールとして、運用保守コストと金銭的コストを最小化してください。

## 6. 外部依存の扱い

重要な外部CLI、API、OS機能、ブラウザ機能、ライブラリは、次をすべて行ってください。

1. バージョンを記録する
2. `--help`や公式ドキュメントでインターフェースを確認する
3. 実データを使った最小PoCを実行する
4. 正常系と代表的な失敗系を確認する
5. コマンド、標準出力、標準エラー、終了コードを保存する

ヘルプ表示だけをPoC完了扱いにしてはいけません。

## 7. コーディング規約

### 共通

- 清潔で可読・保守しやすいコードにしてください。
- 1行1処理を原則としてください。
- 意味のある命名を使ってください。
- 複雑な理由や制約だけをコメントし、コードの逐語説明は避けてください。
- 小規模な個人用ツールに不要なレイヤーを作らないでください。
- 一度しか使わない短い値は不要に変数化しないでください。ただし80〜100文字を超える式や複数戻り値は読みやすさを優先してください。

### Goを採用する場合

- 実装時点の安定版Goを使ってください。
- 標準的な `cmd/vgm` と `internal/` 構成を使ってください。
- `gofmt`、`go test ./...`、`go vet ./...`を通してください。
- 標準ライブラリで十分な箇所へフレームワークを導入しないでください。
- 外部CLI呼び出しでは標準出力、標準エラー、終了コードを明示的に扱ってください。
- 関数は原則30行以内ですが、分割で可読性が落ちる場合は無理に分割しないでください。

### Pythonを採用する場合

- Python 3.12以上を第一候補とし、PyMusicLooperの対応状況を優先してください。
- `pyproject.toml`を正本にし、環境管理は`uv`を第一候補にしてください。
- 型ヒント、`pathlib`、標準`logging`、`pytest`、`ruff`を使ってください。
- 巨大な単一スクリプトへ製品処理を詰め込まないでください。
- PyMusicLooper内部APIを利用する場合は、CLI利用より有利で安定していることをPoCで確認してください。

### HTMLプレイヤー

- プレーンHTML、CSS、JavaScript ES Modulesを基本としてください。
- React、Vue、SvelteなどのUIフレームワークを導入しないでください。
- CSS宣言を1行に詰め込まないでください。
- JavaScriptの関数定義はアロー関数を基本としてください。
- 定数・グローバル状態・エントリーポイント関数を先頭側に置き、エントリーポイント呼び出しを末尾に置いてください。
- 承認済みモックアップの見た目と操作を基準にしてください。

## 8. テストと報告

各段階の報告には最低限、次を含めてください。

- 実施した対象
- 実行したコマンド
- 使用バージョン
- 成功したテスト
- 失敗したテスト
- ログ・生成物の場所
- 仕様どおりかを機械的に確認した結果
- 人間の判断が必要な項目
- GO、NO-GO、または判断待ち

「たぶん動く」「実装したが未実行」は完了ではありません。
