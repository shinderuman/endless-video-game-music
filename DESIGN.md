# DESIGN.md — 設計の正本

## 1. プロジェクト状態

- 名称: **Endless Video Game Music**
- リポジトリ: `endless-video-game-music`
- CLI: `vgm`
- 状態: 設計済み、PoC未完了、製品実装禁止
- 用途: 利用者個人のゲーム音楽コレクション
- 想定規模:
  - 全音源: 約25,000曲
  - ゲーム音楽プレイリスト: 約17,000曲
  - 実際にループ定義へ採用される曲数はPoC後に確定

## 2. 目的

CD収録時に1回または2回のループ後にフェードアウトするゲーム音楽について、事前解析したループ開始・終了位置を使い、MacとAndroidでローカル音源を無限ループ再生できるようにします。

再生時には音源解析を行いません。音源や波形をネットワークへ送信しません。

## 3. 成果物

### 3.1 定義JSON出力CLI

- 実行ファイル名: `vgm`
- 主コマンド: `vgm generate`
- 実行環境: macOS
- 入力:
  - Musicアプリから出力したM3U8プレイリスト
  - M3U8が参照するローカルMP3・M4A音源
- 処理:
  - M3U8に含まれる曲だけを列挙
  - 音源タグからアルバム名、曲タイトル、ディスク番号、トラック番号を取得
  - PyMusicLooperで非対話解析
  - 確定したスコア閾値以上の曲だけを採用
  - PyMusicLooperが検出できるすべてのループ構造を対象とする
  - 同じ作品の複数ディスクを同じアルバムとして構成
  - HTMLプレイヤー用の定義JSONを生成
- 実装言語:
  - PyMusicLooperのPoC結果を見て決定
  - Python直接利用と、GoからCLIを呼ぶ方式のどちらが単純で安定するかをPoC後に選ぶ

### 3.2 静的HTMLプレイヤー

- 対象OS・ブラウザ:
  - macOS Chrome
  - macOS Edge
  - Android Chrome
  - Android Edge
- 初期公開先候補: GitHub Pages
- 音源: 各クライアント端末のローカルファイル
- 定義: 静的に公開された定義JSON
- 必須音源形式: MP3、M4A
- 解析機能: なし
- サーバーAPI: なし

## 4. 全体データフロー

```text
M3U8プレイリスト
  + ローカル音源
        ↓
  vgm generate
        ↓
  PyMusicLooper解析結果
        ↓
  採用曲だけの定義JSON
        ↓
  静的公開
        ↓
HTMLプレイヤー
  + 端末内ローカル音源
        ↓
  定義済み位置で無限ループ再生
```

## 5. 解析対象と採用方針

- プレイリストに含まれる曲だけを解析対象にします。
- プレイリストはゲーム音楽候補を絞るための入力です。
- ゲーム音楽プレイリスト内にもループできない曲が含まれます。
- PyMusicLooperの最上位候補スコアを曲の自動採用判断に使えるかはPoCで検証します。
- 閾値未満は「ループ非対応」と断定せず、**自動判定で未採用**として扱います。
- 採用閾値は利用者が聴感ラベルを付けた結果から決定します。
- PyMusicLooperが検出できる任意のループ開始位置・終了位置を扱います。曲頭へ戻る構造に限定しません。
- 独自ループ検出アルゴリズムは作りません。

## 6. メタデータとアルバム構成

- アルバム名: 音源タグから取得
- 曲タイトル: 音源タグから取得
- ディスク番号: 音源タグから取得
- トラック番号: 音源タグから取得
- アーティスト情報:
  - 検索・分類に使わない
  - Various Artistsや未設定が多いため、UIの正本にしない
- 複数ディスク:
  - 同じ作品のDisc 1、Disc 2などは同じアルバムとして表示する
  - 実データ上の統合規則はPoCで調査し、利用者のGO後に固定する

## 7. 曲とローカルファイルの突合

最終方式は未決定です。製品実装前にPoC 3で確定します。

背景:

- APFS、FAT/exFAT、Androidでファイル名・パス表現が同一文字列になるとは限りません。
- 利用者は`music-bridge`のAndroid対応で、Unicode正規化、予約文字、大文字小文字などの差異へ対応済みです。
- ローカルの調査対象は `~/src/music-bridge` です。

候補:

1. `music-bridge`の既存ポータブルパス規則または既存生成データを再利用する
2. SHA-256を曲識別に利用する
3. PoCで必要と判明した最小限の併用

重要:

- パス方式を先に決めない
- SHA-256方式を先に決めない
- 両方式を最初から製品実装しない
- PoCで実ファイルとAndroidエミュレーターを使い、最小で確実な方式だけを採用する
- 最終定義JSONスキーマは突合方式のGO後に確定する

## 8. HTMLプレイヤーのカタログと検索

プレイヤー画面の正本はローカルフォルダ一覧ではなく、定義JSONです。

- 定義へ採用されたアルバムと曲だけを表示します。
- ボーカル曲や未採用曲は表示しません。
- ローカル25,000曲をそのまま一覧表示しません。
- 検索対象は次の2項目だけです。
  - アルバム名
  - 曲タイトル
- アーティスト名は検索しません。
- 頭文字・五十音フィルタは実装しません。
- 検索文字列は空白で分割し、すべての語がアルバム名または曲タイトルに含まれる結果へ絞ります。
- 空の検索で17,000曲を一括表示しません。
- アルバム名に一致した結果と、曲名・アルバム名に一致した曲結果を分けて表示します。
- 曲結果はアルバム名が分かる形で表示します。
- アルバムを選ぶと、定義に含まれるそのアルバムの曲だけをトラック順に表示します。

承認済みの外観・レスポンシブ設計・キーボード操作は `references/approved-player-mockup.html` を基準にします。

## 9. プレイヤー操作

### 曲選択

- 曲を選択した時点では再生しません。
- 選択曲、アルバム、再生可能状態をプレイヤー領域へ反映します。
- 再生ボタンで開始します。

### 再生・一時停止

- 停止中または一時停止中に再生ボタンを押すと再生します。
- 再生中に同じボタンを押すと一時停止します。
- 再開時は一時停止位置から続けます。

### 停止

- 停止ボタンで再生を停止します。
- 再生位置を曲の先頭へ戻します。

### 前・次

- 選択中アルバム内でだけ移動します。
- アルバムをまたいで移動しません。
- 再生中に前・次へ移動した場合、移動先を再生します。
- 停止中または一時停止中に移動した場合、移動先は停止状態になります。
- 先頭曲では前ボタンを無効化し、「前の曲はありません」と表示します。
- 末尾曲では次ボタンを無効化し、「次の曲はありません」と表示します。
- 先頭と末尾を循環させません。

### ループ

- 定義の開始・終了サンプル位置と解析時サンプルレートを使います。
- ブラウザAPIへ渡す秒位置は、解析時サンプル位置を解析時サンプルレートで割って求めます。
- PyMusicLooperが検出したループ開始位置へ、ループ終了時に戻ります。

## 10. CLIの中断・失敗・再実行・上書き・ログ

### 中間状態

- 1曲の解析が完了するたびに途中状態を保存します。
- 完了済み曲は再実行時に再解析しません。
- プレイリストへ追加された未処理曲だけを新規解析します。
- 以前失敗した曲は再実行時に再試行します。
- 1曲の失敗で全体処理を停止しません。

### 状態区分

最低限、次を区別します。

- 採用
- 基準未満
- 失敗
- 未処理

### 最終JSON

- 成功・採用済みの曲から最終JSONを生成します。
- 一時ファイルへ完全に書き出して検証してから、既存JSONを原子的に置換します。
- 一部失敗があっても成功分で最終JSONを更新します。
- 一部失敗時は正常終了扱いにせず、終了コードとログで一部失敗を示します。
- 失敗曲は次回実行時の再試行対象として残します。
- 不完全な書き込みで既存の正常なJSONを壊しません。

### ログ

最低限、次を記録します。

- 実行開始・終了
- 使用したM3U8
- PyMusicLooperのバージョンと固定オプション
- 総件数、処理済み、採用、基準未満、失敗、未処理
- 曲ごとの開始、結果、スコア、失敗理由
- 中断
- 最終JSONの更新有無
- 終了状態

ログの具体的な保存場所と形式は製品設計確定時に決めます。ログを実装しないという選択はできません。

## 11. 採用技術の方針

### PyMusicLooper

第一候補として採用します。ただし次をPoCで固定します。

- バージョン
- 非対話コマンドとオプション
- 出力形式
- スコアの意味と範囲
- M4A・MP3での実動作
- 1曲あたりの解析時間
- CLI呼び出しとPython内部利用のどちらを採るか

### HTMLプレイヤー

- プレーンHTML、CSS、JavaScript ES Modules
- UIフレームワークなし
- 静的ホスティング
- File System Access APIとWeb Audio APIの実動作をPoCで確認

### Android検証

- 実機必須にはしません。
- `music-bridge`で使っているAndroid Emulator環境を調査し、再利用します。
- ChromeとEdgeをエミュレーター上で確認します。

## 12. 永久対象外

以下は初期版だけでなく、本プロジェクトの対象外です。

- 実行時解析サーバー
- 音源または波形のアップロード
- 自宅Macの常時稼働サーバー
- データベース
- PWA化
- Safari対応
- Firefox対応
- アーティスト検索
- 頭文字・五十音フィルタ
- 波形表示
- 独自ループ検出アルゴリズム
- PyMusicLooper未採用曲の救済UI
- ジャケット画像の取得・表示・管理
- ローカル全音源をそのままカタログ表示する機能

## 13. TODO候補

次だけを将来TODO候補として残します。製品初期実装には含めません。

- 手動ループ位置編集

明示的な追加指示が出るまで、設計も実装も開始しません。

## 14. 製品実装前の未解決事項

PoC結果と利用者のGOで、次を固定する必要があります。

1. PyMusicLooperの採用スコア閾値
2. 曲とローカルファイルの突合方式
3. 定義JSONの最終スキーマと曲ID
4. 複数ディスクを同一アルバムへ統合する実データ規則
5. 製品CLIの実装言語
6. PyMusicLooperの固定バージョンと固定オプション
7. 定義JSONの公開先と更新方法
8. ログと中間状態の具体的な保存場所・形式

未解決事項をCodexが勝手に決めて製品実装してはいけません。

## 15. 製品受け入れ条件

PoC総合GO後に製品実装へ進んだ場合、最低限次を満たします。

### CLI

- `vgm generate`で指定M3U8を処理できる
- M3U8外の曲を解析しない
- MP3とM4Aを解析できる
- タグからアルバム名、曲名、ディスク番号、トラック番号を取得できる
- 確定規則で複数ディスクを同一アルバムへまとめる
- 確定閾値以上の曲だけを定義へ採用する
- PyMusicLooperの開始・終了サンプルと解析時サンプルレートを欠落なく保存する
- 1曲ごとに途中状態を保存する
- 中断後、完了済み曲を再解析しない
- プレイリスト追加曲だけを解析する
- 失敗曲を後続から分離して再試行できる
- 一部失敗でも成功分を原子的に最終JSONへ反映する
- 一部失敗を終了コードとログで識別できる
- 生成JSONが構文的に有効である

### HTMLプレイヤー

- macOS Chrome・Edgeで動く
- Android Emulator上のChrome・Edgeで動く
- 利用者がローカル音源フォルダを選択できる
- 確定した突合方式で定義曲とローカル音源を対応付けられる
- MP3とM4Aを端末内で再生できる
- 音源をネットワークへ送信しない
- 定義に存在しない曲を表示しない
- アルバム名と曲タイトルを空白区切りAND条件で検索できる
- アーティスト、頭文字、五十音による検索を持たない
- 承認済みモックアップと同等の外観・レスポンシブ表示・操作を持つ
- 再生、一時停止、停止、前、次が第9節どおりに動く
- アルバムの先頭・末尾で循環しない
- PyMusicLooperの指定位置で複数回ループを継続できる
- 約17,000曲相当の定義で検索操作が実用的な速度で動く

### 禁止事項

- 永久対象外機能が混入していない
- PoCで採用されなかった照合方式を保険として併設していない
- 製品コードにPoC専用の仮実装が残っていない
