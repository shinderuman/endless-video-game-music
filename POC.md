# POC.md — PoC手順・段階別実装指示

## 1. 共通ルール

- 各Stageは独立したゲートです。
- 各Stage完了後に`POC_RESULTS.md`を更新し、利用者へ報告して停止します。
- 利用者の明示的な許可なしに次Stageへ進みません。
- Stage 5までGOになり、利用者が総合GOを出すまで製品実装を開始しません。
- PoCコードは`poc/`配下に置き、製品コードとして流用しません。
- 外部依存はヘルプ確認だけでなく実データで動かします。
- 可能な結果は自動テスト、JSON検証、終了コード、ログ比較などで機械的に検証します。
- 聴感判定など人間にしか判断できない部分だけ利用者へ提示します。

## Stage 0: 実行環境・既存資産調査

### 対象

- macOS実行環境
- Python、uv、PyMusicLooper
- ffmpeg、ffprobe
- M3U8参考入力
- `~/src/music-bridge`
- Android SDK、ADB、Android Emulator
- エミュレーター上のChrome

### 実施内容

1. 全文書を読む
2. 次のコマンドを実行し、出力と終了コードを保存する
   - `python3 --version`
   - `uv --version`
   - `pymusiclooper --version`または同等の確認
   - `pymusiclooper --help`
   - `pymusiclooper export-points --help`
   - `ffmpeg -version`
   - `ffprobe -version`
   - `adb version`
   - Android SDK・AVD一覧確認
3. `~/src/music-bridge`を調査する
   - `git status`
   - 現在のブランチとHEAD
   - コミット`0f12fa6b3c8351e7e4a54fbbb11c14ea5af86711`
   - `internal/portable`
   - Android関連テスト
   - AVD自動起動・停止処理
   - 既存のテストデータ、manifest、M3U、論理パス規則
4. 安全に不足依存を導入できる場合は自分で導入し、再確認する

### 対象外

- PyMusicLooper解析の本実行
- PoCコード作成
- 製品コード作成

### 完了条件

- Stage 1〜5を実施可能か判断できる
- 利用可能な既存資産と不足が一覧化されている
- 実行したコマンドとバージョンが`POC_RESULTS.md`に記録されている

### GO

PoCに必要な環境をCodex自身で準備できる、または不足が明確で解消手順が確定していること。

### NO-GO

重要な依存を利用できず、Codex自身で解消できず、代替も要件を満たさないこと。

### 報告後

停止する。Stage 1へ進まない。

---

## Stage 1: PyMusicLooper自動判定PoC

### 目的

PyMusicLooperの最上位候補スコアを、曲横断の自動採用閾値として利用できるか確認します。

### 入力

1. 利用者が聴き慣れたゲーム音楽アルバムから再現可能に抽出する既知ループ候補
2. 利用者が曲の性質を一括判断できる、ゲーム音楽入力外の既知非ループ対照群
3. 完全版M3U8から再現可能に無作為抽出する探索用標本

無作為標本だけでは、聴き慣れていない曲の正解ラベルが曖昧になる。そのため、
閾値評価の主標本には既知ループ候補と既知非ループ対照群を使い、無作為標本は
スコア分布と実行安定性を見る探索用として扱う。対照群は製品の解析対象へ追加しない。

### 手順A: 大まかなスコア領域確認

1. 利用者指定曲を非対話で解析する
2. 次を記録する
   - PyMusicLooperのバージョン
   - 実行コマンドと固定オプション
   - 標準出力、標準エラー、終了コード
   - ループ開始サンプル
   - ループ終了サンプル
   - スコアの生値
   - 候補なし・失敗の区別
   - 処理時間
3. スコア基準が概ね30、50、70のどの領域になりそうかだけ報告する

### 手順B: 再現可能な評価標本

1. 利用者が指定した既知ループ候補アルバムごとに少数曲を抽出する
2. 利用者が一括判断できる既知非ループ曲群から対照標本を抽出する
3. 抽出の乱数シード、対象アルバム、対象プレイリストを記録し、再現可能にする
4. 全曲を同一バージョン・同一オプションで解析する
5. 次の機械可読JSONを生成する

```json
{
  "sampleSeed": 12345,
  "pymusiclooperVersion": "RECORD_ACTUAL_VERSION",
  "commandOptions": ["RECORD", "ACTUAL", "OPTIONS"],
  "tracks": [
    {
      "sourcePath": "/absolute/path/to/track.m4a",
      "album": "Example Album",
      "title": "Example Track",
      "loopStartSample": 123456,
      "loopEndSample": 2345678,
      "scoreRaw": "RECORD_EXACT_OUTPUT",
      "durationSeconds": 12.34,
      "analysisStatus": "candidate",
      "userLabel": null
    }
  ]
}
```

6. 利用者が確認しやすい一覧も生成する
7. 利用者へ各曲を次のいずれかでラベル付けしてもらう
   - ループ対応曲
   - ループ非対応曲
   - おそらくループ対応だが、現在のループ開始・終了点は不正
8. ラベル受領後、閾値ごとの誤採用・誤除外を集計する
9. `loop_bad_points`について、ループ終了位置から音源末尾までの秒数を集計し、
   フェードアウト領域にある候補を補助条件で除外できるか確認する
10. 閾値候補を提示するが、Codexが勝手に確定しない

別途、完全版M3U8から約100曲をシード付きで無作為抽出し、解析の成功率、
処理時間、候補スコア分布を記録する。利用者が確信を持って判定できた分の
ラベルは探索情報として保存するが、全件ラベルをStage 1の完了条件にはしない。

### 実行上の注意

- `export-points`などの正確なコマンドと出力形式は、インストール済みバージョンのヘルプと実出力を正本にする
- 公式README上の例では、全候補出力は`loop_start loop_end note_difference loudness_difference score`形式だが、必ず実バージョンで確認する
- スコアが0〜1か百分率かを推測せず、生値を保存する
- PyMusicLooperが検出できる任意のループ構造を対象とする
- 独自判定器を作らない

### 対象外

- 17,000曲の本番解析
- 最終閾値の自動確定
- 誤判定曲の手動編集UI
- 独自ループ検出

### 完了条件

- 少数の決定的な曲で大まかなスコア領域が分かる
- 既知ループ候補と既知非ループ対照群の解析結果が再現可能な形で生成される
- 既知ループ候補への利用者ラベルを反映した閾値別集計がある
- 約100曲の無作為探索標本について、解析成功率、処理時間、スコア分布が記録される
- 1曲あたりの時間と17,000曲の概算時間がある

### GO

利用者が、特定の単一閾値を製品の自動採用基準として明示的に承認すること。

### NO-GO

単一閾値では実用上受け入れられず、利用者が設計変更も承認しないこと。

### 報告後

停止する。Stage 2へ進まない。

---

## Stage 2: M3U8・タグ・複数ディスク統合PoC

### 目的

実際のM3U8と音源タグから、プレイヤー表示用のアルバム・曲順を安定して構成できるか確認します。

### 手順

1. 完全版M3U8をUTF-8として解析する
2. コメント行、`#EXTINF`、絶対パスの対応を確認する
3. 音源から次のタグを取得する
   - アルバム名
   - 曲タイトル
   - ディスク番号
   - トラック番号
4. 複数アーティストの曲をアーティスト単位に分割しない
5. Disc 1、Disc 2など同じ作品を同一アルバムへ統合するため、実際のタグ値を調査する
6. 最小の統合規則案を作り、複数の実アルバムで結果を示す
7. ディスク番号、トラック番号の順で並べる
8. 試験用データを生成する
   - この段階の曲識別子は仮でよい
   - 最終JSONスキーマは確定しない

### 対象外

- 最終JSONスキーマ確定
- 製品CLI実装
- 曲突合方式の確定
- アーティスト検索

### 完了条件

- M3U8内の対象曲を漏れなく列挙できる
- 必須タグの取得成功率と失敗例が分かる
- 複数ディスクの統合結果を実データで確認できる
- 誤統合、未統合、タグ欠落が一覧化されている

### GO

利用者が実データの統合結果と規則を承認すること。

### NO-GO

タグだけでは要求どおりのアルバム構成を作れず、利用者が最小の補正規則も承認しないこと。

### 報告後

停止する。Stage 3へ進まない。

---

## Stage 3: Mac・Android間の曲突合PoC

### 目的

APFS、FAT/exFAT、Android間で、定義上の曲と端末内の同一音源を最小の実装で確実に突合する方式を決定します。

### 最初に調査するもの

- `~/src/music-bridge`
- コミット`0f12fa6b3c8351e7e4a54fbbb11c14ea5af86711`
- `internal/portable`
- `internal/layout`
- manifestや相対パスM3U
- Android Emulator統合テスト
- 既存の予約文字、Unicode、大文字小文字、部分転送、差分同期のテスト

### 過去Codexセッションの扱い

1. Codexが過去セッションを直接参照できる場合、`music-bridge`のAndroid対応時の設計判断と失敗履歴を調査する
2. 直接参照できない場合、今回のCodex AがCodex Bへ確認したい質問を具体的に作る
3. 質問を`POC_RESULTS.md`の「Codex B質問票」へ記録し、利用者による中継を待つ
4. Codex Bの回答は、現在の`~/src/music-bridge`のソース、Git履歴、テストと照合する
5. 過去回答だけを根拠に採用方式を決めない

### 比較順序

1. `music-bridge`の既存論理パス・manifest・正規化規則を、そのまままたは最小変更で利用できるか試す
2. 既存資産だけで確実に突合できない場合に限り、SHA-256索引の最小PoCを行う
3. 必要性が証明されない限り、パスとSHA-256の併用製品案を作らない

### 検証データ

- `music-bridge`の既存テストデータ
- APFSからFAT/exFATまたはAndroidへ同期した実ファイル
- Unicode正規化差を含む名前
- FAT/exFAT予約文字を含む名前
- 大文字・小文字差を含む名前
- Android Emulator上の実際のファイル列挙結果

### Android環境

- 実機は必須にしない
- `music-bridge`が使う既存AVD、ADB、テスト起動・停止処理を再利用する
- Chromeでファイル列挙結果を確認する

### 機械的検証

- 定義側の対象集合とAndroid側の突合結果を比較する
- 一致、未一致、重複一致を件数で出す
- 既知の難しいファイル名を個別アサートする
- SHA-256を試す場合、索引作成時間、読込量、再利用方法を計測する

### 対象外

- 製品プレイヤー実装
- 2方式の同時製品実装
- 一般ユーザー向けの任意ライブラリ互換性

### 完了条件

- 候補方式ごとの一致率、処理時間、実装量が分かる
- 未一致と重複一致の具体例がある
- 最小の採用候補が1つ示される
- 定義JSONで必要になる曲ID情報が説明される

### GO

利用者が1つの曲突合方式を明示的に承認すること。

### NO-GO

現実的な初回時間と実装量で確実な突合ができず、利用者が設計変更を承認しないこと。

### 報告後

停止する。Stage 4へ進まない。

---

## Stage 4: HTMLプレイヤー実動作PoC

### 目的

対象2環境で、ローカルフォルダ選択、曲突合、MP3/M4Aデコード、任意区間ループが成立するか確認します。

### PoCコード

- `poc/player/`配下に最小コードを置く
- 製品コードとして扱わない
- 承認済みモックアップの全面実装は行わない
- 必要な操作だけを確認する

### 対象環境

- macOS Chrome
- Android Emulator Chrome

Edgeは互換動作を意図しますが、このPoCの必須検証対象には含めません。

### 手順

1. GitHub Pages相当の安全なコンテキストでPoCを配信する
2. ローカル音源フォルダを選択する
3. Stage 3で承認された方式で既知の曲を突合する
4. MP3をデコード・再生する
5. M4Aをデコード・再生する
6. PyMusicLooperの開始・終了サンプルと解析時サンプルレートからループ位置を設定する
7. 複数回ループを継続する
8. 再生、一時停止、停止、前、次、先頭、末尾を確認する
9. 約17,000曲相当の定義ダミーデータで検索時間と描画時間を測る
10. PCブラウザの狭幅とAndroid Emulatorでレスポンシブ表示を確認する

### 自動確認

- JSON読込成功
- 対象曲の一意な突合
- 再生状態遷移
- 停止時の位置リセット
- 前後移動の境界
- ループ終端を複数回通過したこと
- ネットワークリクエストへ音源データが送られていないこと
- 17,000曲相当検索の計測値

### 対象外

- 製品版デザイン完成
- PWA
- Safari・Firefox
- バックエンドAPI
- 波形表示
- 手動ループ編集

### 完了条件

- 2対象環境で必須操作が動く
- MP3・M4Aで複数ループする
- 曲突合が成功する
- 17,000曲相当で検索が実用可能と判断できる測定値がある
- 失敗が再現可能な形で記録される

### GO

利用者がHTML方式を製品構成として明示的に承認すること。

### NO-GO

対象環境のいずれかで必須API・音源形式・ループ再生・曲突合が成立せず、最小修正でも要件を満たさないこと。

### 報告後

停止する。Stage 5へ進まない。

---

## Stage 5: 中断・失敗・再実行PoC

### 目的

17,000曲規模の長時間処理を、全曲やり直さず安全に継続できることを確認します。

### PoCコード

- `poc/generator/`配下に最小コードを置く
- 10〜数十曲で状態遷移だけを確認する
- 製品CLIを実装しない

### 必須シナリオ

1. 数曲成功後に処理を中断する
2. 再実行し、完了済み曲を再解析しない
3. M3U8へ曲を追加し、追加曲だけ解析する
4. 1曲を意図的に失敗させ、後続曲を処理する
5. 再実行で失敗曲だけ再試行する
6. 採用、基準未満、失敗、未処理を区別する
7. 一部失敗状態で成功分の最終JSONを生成する
8. 一時ファイルを検証後に既存JSONへ原子的に置換する
9. 書き込み途中の強制終了で既存JSONが壊れないことを確認する
10. ログと終了コードで完全成功、一部失敗、中断を区別する

### 対象外

- 17,000曲の本番実行
- 最終CLI構成
- GUI進捗画面
- データベース

### 完了条件

- すべての必須シナリオを自動テストで再現できる
- 再解析された曲がないことをログまたは呼出回数で検証できる
- 既存の正常な最終JSONが破損しない
- 一部失敗でも成功分が反映される

### GO

利用者が中断・再実行・一部失敗・上書き・ログの挙動を承認すること。

### NO-GO

完了済み曲の再解析または最終JSON破損を避けられないこと。

### 報告後

停止する。製品実装へ進まない。

---

## Stage 6: PoC総合判定と設計固定

### 対象

- Stage 1〜5の結果
- すべての利用者GO
- `DESIGN.md`の未解決事項

### 手順

1. PoC結果を統合する
2. 次を`DESIGN.md`へ反映する案を作る
   - スコア閾値
   - PyMusicLooper固定バージョン・オプション
   - CLI実装言語
   - 曲突合方式
   - JSONスキーマ
   - 複数ディスク統合規則
   - ログ・中間状態の具体形式
3. 変更差分を報告する
4. 製品実装の段階計画を確定する

### 完了条件

- 製品実装担当が重要仕様を推測しなくてよい
- `DESIGN.md`に未解決の必須事項が残らない

### GO

利用者が総合GOと設計更新を明示的に承認すること。

### NO-GO

必須PoCにNO-GOが残ること。

### 報告後

停止する。製品実装へ進まない。

---

# 総合GO後の段階別製品実装指示

以下は総合GO後だけ有効です。現時点では実行禁止です。

## Product Stage 1: 設計文書更新

### 対象

- PoCで確定した設計だけ

### 対象外

- 製品コード

### 完了条件

- `DESIGN.md`、受け入れ条件、JSONスキーマ、CLI仕様が確定
- 未解決の必須事項がない

### テスト

- JSON例の構文検証
- 文書間整合性チェック

### 報告

- 変更一覧
- 残存TODO

完了後停止する。

## Product Stage 2: 定義JSON出力CLI

### 対象

- `vgm generate`
- M3U8解析
- タグ読取
- PyMusicLooper連携
- 中間状態、再実行、ログ、原子的JSON更新

### 対象外

- HTMLプレイヤー
- 永久対象外機能

### 完了条件

- `DESIGN.md`のCLI受け入れ条件をすべて満たす

### テスト

- 単体テスト
- 統合テスト
- 実音源PoC回帰テスト
- 中断・失敗・再実行テスト
- JSON構文・スキーマ検証

### 報告

- 実行コマンド
- テスト結果
- 実音源での結果
- 未解決事項

完了後停止する。

## Product Stage 3: HTMLプレイヤー

### 対象

- 承認済みモックアップに基づくUI
- 定義JSON検索
- ローカルフォルダ選択
- 曲突合
- MP3・M4Aループ再生
- 必須プレイヤー操作

### 対象外

- 永久対象外機能
- 手動ループ編集
- サーバーAPI

### 完了条件

- `DESIGN.md`のHTMLプレイヤー受け入れ条件をすべて満たす

### テスト

- JavaScript単体テスト
- ブラウザ自動テスト
- macOS Chrome
- Android Emulator Chrome
- 17,000曲相当の性能テスト

### 報告

- 対象環境ごとの結果
- 操作動画または自動テスト証跡の場所
- ネットワーク検査結果
- 未解決事項

完了後停止する。

## Product Stage 4: 統合受け入れ

### 対象

- CLI生成JSONをHTMLプレイヤーで使用する一連の流れ

### 対象外

- 新機能追加

### 完了条件

- 実M3U8から定義生成
- 端末内音源と突合
- 対象2環境で無限ループ再生
- 中断・再実行・一部失敗を含む全受け入れ条件合格

### テスト

- E2E
- 回帰テスト
- ZIPまたはリリース生成物の展開テスト

### 報告

- 合否一覧
- 既知の制約
- リリース可否

完了後停止する。
